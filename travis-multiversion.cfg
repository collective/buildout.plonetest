[env]
recipe = gocept.recipe.env

[buildout]
extends =
    test-${env:PLONE_VERSION}.x.cfg
    buildout-cache.cfg

parts = download install test
plone-series = ${env:PLONE_VERSION}
package-name =
package-extras =
test-eggs =
