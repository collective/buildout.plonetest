[variables]
recipe = buildout.variables
plone-series = ${env:PLONE_VERSION}

[env]
recipe = gocept.recipe.env

[buildout]
extends =
    test-${variables:plone-series}.x.cfg
    buildout-cache.cfg

parts = download install test
plone-series = ${variables:plone-series}
package-name =
package-extras =
test-eggs =
